Config = {}

Config.Locale = 'es' --Language of the script / Lenguaje del script

Config.SetDimensionCommand = 'setdimension' --Command to set a dimension to a player / Comando para poner una dimension a un jugador
Config.SetDefaultDimension = 'autofix' -- Give Back to default Dimension / Vuelve a la dimension por defecto
Config.ShowDimensionCommand = 'showdimension' -- Show the actual dimension / Te muestra la dimension actual del jugador